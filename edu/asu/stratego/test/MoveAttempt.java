package edu.asu.stratego.test;
import edu.asu.stratego.*;
import edu.asu.stratego.gui.*;
import edu.asu.stratego.game.*;
import static org.junit.Assert.*;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class MoveAttempt {
/*	private Server ser;
	public static Client red;
	public static Client blue;
	private ClientStage cl_red;
	private ClientStage cl_blue;

	private class Spawner implements Runnable {

		@Override
		public void run() {
			// TODO Auto-generated method stub
			MoveTest.red  = new Client();
			red.main(null);
			MoveTest.blue = new Client();
			blue.main(null);
		}
		
	}
	
	@Before
	public void setUp() throws Exception {
		new Thread(new Spawner());
		Server.main(null);
	}

	@After
	public void tearDown() throws Exception {
		System.out.println("done");
	}

	@Test
	public void test() {
		while (red.getGame().getStatus() == GameStatus.IN_PROGRESS || red.getGame().getStatus() == GameStatus.SETTING_UP
				|| red.getGame().getStatus() == GameStatus.WAITING_OPP ) {
			
		}
		fail("Not yet implemented");
	}
*/
}
